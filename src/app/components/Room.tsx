// import useSocket from '../hooks/useSocket';

// interface RoomProps {
//   room: string;
//   username: string;
// }

// const Room: React.FC<RoomProps> = ({ room, username }) => {
//   const { socket, users } = useSocket(room, username);

//   return (
//     <div>
//       <h1>Room: {room}</h1>
//       <h2>Users in this room:</h2>
//       <ul>
//         {users.map((user) => (
//           <li key={user.id}>{user.username}</li>
//         ))}
//       </ul>
//     </div>
//   );
// };

// export default Room;
